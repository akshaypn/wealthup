/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['100.123.199.100'],
  },
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'Referrer-Policy',
            value: 'origin-when-cross-origin',
          },
        ],
      },
    ];
  },
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: `${process.env.NEXT_PUBLIC_API_URL || 'http://100.123.199.100:9001'}/api/v1/:path*`,
      },
    ];
  },
};

module.exports = nextConfig; 